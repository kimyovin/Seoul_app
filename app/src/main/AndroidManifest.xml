<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bokjirock">

    <application
        android:allowBackup="true"
        android:icon="@drawable/init_image"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity
            android:name=".introActivity"
            android:label="복지樂">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".searchActivity" android:label="조건 검색"/>-->
        <!--<activity android:name=".categoryActivity" android:label="카테고리 검색"/>-->
        <!--<activity android:name=".locationActivity" android:label="위치 검색"/>-->
        <activity android:name=".MainActivity" android:label="복지樂"/>
        <activity android:name=".resultActivity" android:label="검색 결과"/>
        <activity android:name=".detailInfo" android:label="상세 결과"/>

        <meta-data android:name="com.kakao.sdk.AppKey" android:value="41fb30bcda59fdf7f241683130ad5689"/>
        <uses-library android:name="org.apache.http.legacy" android:required="false" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION">
    </uses-permission>

</manifest>




